name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Java JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Force Generate Accurate google-services.json
        run: |
          rm -f app/google-services.json
          # Restore debug keystore from base64 if present
          if [ -f "debug.keystore.base64" ]; then
            base64 -d debug.keystore.base64 > debug.keystore
          fi
          cat << 'EOF' > app/google-services.json
          {
            "project_info": {
              "project_number": "100000000000",
              "firebase_url": "https://rakib-ai-engine-default-rtdb.firebaseio.com",
              "project_id": "rakib-ai-engine",
              "storage_bucket": "rakib-ai-engine.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:100000000000:android:abcdef123456",
                  "android_client_info": {
                    "package_name": "com.aistudio.keyauth.qrxkit"
                  }
                },
                "api_key": [
                  {
                    "current_key": "AIzaSyD-FakeKeyForBuildingPurposesOnly123"
                  }
                ],
                "services": {
                  "appinvite_service": {
                    "other_platform_oauth_client": []
                  }
                }
              },
              {
                "client_info": {
                  "mobilesdk_app_id": "1:100000000000:android:abcdef123457",
                  "android_client_info": {
                    "package_name": "com.aistudio.keyauth.qrvklt"
                  }
                },
                "api_key": [
                  {
                    "current_key": "AIzaSyD-FakeKeyForBuildingPurposesOnly123"
                  }
                ],
                "services": {
                  "appinvite_service": {
                    "other_platform_oauth_client": []
                  }
                }
              }
            ],
            "configuration_version": "1"
          }
          EOF

      - name: Make Gradle Executable
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug --no-daemon -x test -x lint -x validateSigningDebug

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: KeyManager-Admin-APK
          path: app/build/outputs/apk/debug/*.apk
